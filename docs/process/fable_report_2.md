# Fable's second report: testing, hand-offs, verbiage, compaction (2026-09-25)

Compaction, plainly. Nothing on disk is at risk: every repo edit is committed at once (44 ledger_main commits today), worktrees are separate checkouts, every workflow's result persists in tasks/<id>.output, engine results in %TEMP%\warships_rungs. A compact loses only what lives in the conversation: a verdict read but not yet written to the ledger, the exact next action, an uncommitted ledger edit (observed once: a 10-line ledger change sat uncommitted for 4 minutes).
The method: (1) when a result lands, the coordinator's FIRST call moves its row Running -> Landed with the verdict line and the one action, and commits; Next 1 is always the exact next call. (2) The hook (tools/lanes.ps1, rewritten) prints a STATE line, checks every task id in Running against its output file and prints ** LANDED, NOT IN LEDGER ** when the ledger lags, plus the live workflows' running agents; it shrinks from 52 lines to ~20 and drops the stale chains. (3) A PreCompact hook commits a dirty ledger_main; a Stop hook refuses to end a reply while the ledger is uncommitted or a result is unrecorded. (4) You compact whenever you like; the hook restores the state and the coordinator trusts it over the summary. (5) Compact once now: the lean CLAUDE.md (bccdadd, 12:56) has reached no agent yet; every agent spawned since still inherits the coordinator's cached 17.8 KB copy.

Reply budget: at most 4 lines, no bullets or headers unless you asked for a list; the first sentence answers, a number is a number. A critic's or gate's report reaches you as 3 lines (found / changes now / your decision with its default) plus the file's path. Today 236 of 525 replies broke the 4-line rule; the retrospective counts them per phase from now on.

Change now (before test-phase.js launches; a script edit and two tool edits, no engine):
1. rungs.ps1 writes <n>_<step>.fails.txt (UTF-8, every verdict line, a count header) and puts the counts in the summary. Today's step logs are UTF-16 and the summary keeps 12 lines: triage saw 12 of 42 reds and a bash grep sees 0. Saves 1-2 rounds (1.5-3M tokens, ~1 h each).
2. The runner -> triage -> fix hand-off is pointers: the fails file, the check method, the seed, the literal to assert (expect); no pasted log excerpts. A kind-check fix returns asserted and merges only when it equals expect and the commit's Checks: line names the method. Closes the one way a red turns green without the game changing.
3. Three seeds for a rewritten check and for a red seen at one seed only (16 of ~48 round-1 signatures appear at one seed); a 6-minute seed sweep (solo x3, six x1 at fresh seeds) after the first green round. Flakes are found at 2 min instead of at the 15-min bar.
4. Fails downstream of a thrown lane are deferred to the next round (after); a run whose red count stops shrinking after merges stops as "not converging" instead of running six rounds; a stopped fix agent gets one continuation from its ledger; fix lanes reuse 4 warm worktrees (quick took 178 s in a cold tree, 78-86 s warm). Saves 1-2 Opus fix lanes per throw and ~100 s per fix.
5. Budgets enforced by the scripts: an agent return is a 200-char verdict + a ledger pointer (returns ran 6-35 lines, 1-3.8 KB); a gate returns file:line + old/new text; a prompt is at most 1,500 chars pointing at the role doc (the re-pasted rules go); a workflow's own return is under 20 lines (today up to 96 KB, read whole at every wake). A wake costs ~3k tokens instead of 15-25k.
6. Every merged fix is one row in docs/plans/ledger_test.md (round, task, kind, seeds proved, traces to, commit); fix ledgers are deleted in the merge. Round 2's triage reads one table, not N ledgers.
7. agents.ps1 -Tokens sums usage per agent and per workflow from the transcripts (the journals carry none). First numbers: one kits6a writer 40.5M tokens in / 81k out over 208 turns; its gate 16.7M. The 1.5-3M-per-round guess becomes a measurement.
8. The retrospective rule (coordinator.md): after every phase one Fable agent reads the journals, ledgers and -Tokens and proposes at most 8 CLAUDE.md/process edits with numbers; applied at the next batch boundary; reported to you in 3 lines.

At the release, in the release commit: the CHANGES fold (Handoff 20 lines, Unreleased at most 150; today the Handoff is 625 lines / 53 KB, re-read after each of 11 compacts at ~13k tokens each), the two-machine test as a 6-line script in NOTES.txt, and verify.ps1 caps (Handoff 20, Unreleased 150, ledger head 40 / POST 6, ledger_main 25) so an overrun goes red at the job that caused it.
After the release: the scenarios lane (a scenario table in the harness reached by scenario=<id>, tools/scenarios.py, the heavies x bosses x lights matrix as its rows) so your standing asks stop being owed prose. The 3+ audit stays off (your ruling: the count is Fable's judgment); if ever turned on it maps first (Haiku) and writes only gaps, after a green round.

Your decisions (default = what I do):
- The Stop hook that blocks a reply while the ledger lags (~1 s per reply): default yes.
- A hook that blocks replies over 6 lines: default no; the retrospective counts them and reports the number.
- Three seeds per rewritten check (+2 min each) and the seed sweep (+6 min once): default yes.
- Delete the 68 MB tasks/bayopz1de.output (a runaway art-lane loop, harmless): default yes.
Files: scratchpad/fable2/{CLAUDE.md, CLAUDE.diff.md, process/, tools/lanes.ps1, tools/agents.ps1, tools/settings.json, tools/rungs.fails.md, tools/test-phase.edits.md, docs/plans/ledger_main.template.md}.
