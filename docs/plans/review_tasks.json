{
 "source": "fable-review wf_3bcc353d-382 (2026-09-25)",
 "tasks": [
  {
   "id": "cap-R1",
   "title": "Host HookWatch ignores a guest DD's warp charge: rip at the charge, not at 5 s",
   "kind": "review",
   "rung": "six",
   "checks": "LaneA6aGrapnelGuestChecks (add a run), LaneA6aGrapnelRipChecks run 2 (unchanged, host-own only)",
   "evidence": "scripts/PlayerShip.cs:1417 `if (Pinned || Disabled || Charging) CastOffHook(def.Id, rip: true);` -- `Charging => _drive.Held >= 0` (PlayerShip.cs:1436) is the OWNER's charge; on the host a guest's charge arrives as the report bit `_drive.Remote = warping` (PlayerShip.cs:2346). So for a guest destroyer the owner ends its move HelmEnd.Drive (HelmMoves.cs:180) but the host keeps the slot marked (ward 450 u/s up, HelmMoves.cs:220-224) and rips only at the slot's Expire 5 s after the press (Abilities.cs:375), against the card ('the DD's own warp ... rip', kits_v31 §3.3) and A6a-9. FIX (one line, PlayerShip.cs:1417): `if (Pinned || Disabled || Charging || _drive.Remote) CastOffHook(def.Id, rip: true);`. CHECK: in LaneA6aGrapnelGuestChecks after the E cast-off block, hook the dummy again (Cool = 0 first), wait 0.6-1.2 s into the swing, `me.DriveHeld = true; me.PressDrive();` and assert `await RealTime(() => Chunks().Count > before, 1.5)` lands the host's chunk here within 1.5 s (long before the 5 s), `me.Helm.Ended == HelmEnd.Drive`, then `me.DriveHeld = false` and the cooldown arriving (Cool > 14.5).",
   "owner": "lane kits6a J9 (HookWatch), gate fix J11",
   "files": "scripts/PlayerShip.cs, tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 4,
   "kept": true
  },
  {
   "id": "cap-R2",
   "title": "LaneA4TargetsChecks: 2nd and 3rd Long Lance presses refused COOLING, Picked never updates",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA4TargetsChecks",
   "evidence": "tools/smoketest/SmokeTest.cs.txt:10452-10458: the first loop calls `dd.UseAbility(\"longlance\", 0, picks)` three times; UseAbility runs `def.Refuse` (PlayerShip.cs:612) before DoAbility, and Ab.LongLance.Refuse is `s.Sl(\"longlance\").Cool > 0 ? \"COOLING\"` (Abilities.cs:343) while FireAlong set Cool = 18 s on the first press (PlayerShip.cs:1540). Presses k=1 and k=2 return at :612 without Pick(), so `Seen()` still holds one id and `Seen().SequenceEqual(picks)` with 2 / 3 picks is false: a deterministic red on every seed (the other two loops go through doAb.Invoke, which skips Refuse, and pass). FIX at :10455: insert `dd.Sl(\"longlance\").Cool = 0;` before `dd.UseAbility(\"longlance\", 0, picks);`.",
   "owner": "lane kits4 J5 (gate fix)",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 3,
   "kept": true
  },
  {
   "id": "cap-R3",
   "title": "Add the Supercarrier-through-a-warp check (patrol returns to its ring after a 2400 u jump)",
   "kind": "review",
   "rung": "solo",
   "checks": "NEW LaneA6aSuperWarpChecks (called right after LaneA6aSuperChecks in the solo list)",
   "evidence": "Card carrier.md Identity: 'the patrol is leashed to the carrier and flies back after a jump (~7 s from 2400 u)' and kits6a job J6 listed LaneA6aSuperWarpChecks; ledger_kits6a.md J6 POST 'Not built: a warp-return check'. Neither WingsPatrolChecks (SmokeTest.cs.txt:12942) nor LaneBWarpChecks' carrier block (:3469, fighters only) moves the carrier with the patrol up. FIX: new `async Task LaneA6aSuperWarpChecks(Vector2 yonder)`: SetClass(Carrier); 3 runs at VaryNear(yonder,300)/VaryAngle heading with `s.Sl(\"super\").Left = 0; Cool = 0; s.Sl(\"warp\").Cool = 0`; `s.UseAbility(\"super\", 0)`; `await RealTime(() => Ps().Count(w => w.FighterPhase == \"Circle\") >= 3, 12)`; then `s.DriveHeld = true; s.PressDrive();` until `s.ChargeHeld >= Vary(3.6f, 4.0f)` (2080-2400 u, inside warp_safe: no disable), `s.DriveHeld = false`, wait until moved > 1500 u; from that frame assert within 9.0 s (`await RealTime(() => Ps().Count == 3 && Ps().All(w => !w.Done && w.Position.DistanceTo(s.Position) <= 300f + 40f), 9)`) all 3 patrol craft are back on the 300 u circle round the NEW position, none Done, `s.Sl(\"super\").Left > 0`; teardown `foreach (var w in Ps()) w.SendOut(null, s.Clock)`.",
   "owner": "lane kits6a J6",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "cap-R4",
   "title": "Gunships' 'else the selected one' path has no check",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6aGunshipChecks (add a block)",
   "evidence": "Card carrier.md E: 'Up to 3 picked targets (else the selected one)'; Abilities.cs:433 and PlayerShip.Launch :867 branch on `_picked.Count > 0 ? _picked : selected`; Hub.cs:1868 passes `Selected?.NetId ?? 0` with the picks. Every run of LaneA6aGunshipChecks (SmokeTest.cs.txt:6850-6851, 6873-6874) passes picks; the selected-only branch is never exercised. FIX: after the refusal block (:6882) add: `{ var at = VaryNear(yonder, 300f); s.Position = at; s.Velocity = Vector2.Zero; s.Sl(\"gunships\").Cool = 0; s.Sl(\"gunships\").Left = 0; await Frame(); var sel = Held(at + Vector2.Right.Rotated(VaryAngle()) * Vary(800f, 2600f)); await Frame(); H.ClearSelection(); H.SelectTarget(sel); s.UseAbility(\"gunships\", sel.NetId, System.Array.Empty<int>()); double tp = s.Clock; while (s.Clock < tp + 3.0) { s.Position = at; s.Velocity = Vector2.Zero; await Frame(); } int on = Gs().Count(w => Mathf.Abs(w.Position.DistanceTo(sel.Position) - 240f) <= 25f); Check(Gs().Count == 2 && on == 2 && Math.Abs(s.Sl(\"gunships\").Cool - 25 + 3) < 0.3, $\"gunships with no pick but a selected target {sel.Position.DistanceTo(at):0} u off: {Gs().Count} craft (2), {on} on its 240 u circle (2)\"); sel.TakeDamage(1e12); H.ClearSelection(); }`.",
   "owner": "lane kits6a J5",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "cap-R5",
   "title": "LaneA6aLanceChecks times the Lance's speed by frames (assumes 60 fps)",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6aLanceChecks",
   "evidence": "tools/smoketest/SmokeTest.cs.txt:7216-7217: `for (int i = 0; i < 10; i++) { ... await Frame(); } float spd = ... (l.GlobalPosition - p0).Length() * 6f` -- 10 frames read as 1/6 s; a frame-rate dip to 55 fps reads 185 u/s against the +-3 tolerance (a clock timed from frames, CLAUDE.md §6). FIX: at :7215 also take `double t0 = s.Clock;` and at :7217 compute `float spd = l != null && GodotObject.IsInstanceValid(l) && s.Clock > t0 ? (float)((l.GlobalPosition - p0).Length() / (s.Clock - t0)) : 0f;` (the Frame loop unchanged).",
   "owner": "lane kits6a J7",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "cap-R6",
   "title": "A guest's rip damage on the host is never asserted (only its chunk on the guest)",
   "kind": "review",
   "rung": "six",
   "checks": "LaneA6aGrapnelHostChecks (NEW, host role, beside LaneA6aSuppressHostWatch) / LaneA6aGrapnelGuestChecks",
   "evidence": "ledger_kits6a.md J9 POST promised 'rung 5 LaneA6aGrapnelHost/GuestChecks (host damage; the chunk on a guest)'; only LaneA6aGrapnelGuestChecks exists (SmokeTest.cs.txt:9503) and it reads the chunk (Fx.Tear, PlayerShip.cs:1428) which is raised BEFORE Dealt.Deal (:1429): a Deal that lands nothing would still pass. LaneA6aGrapnelPartyRipChecks (:9544) is the host's OWN ship. FIX: a LateSampler on the host started with the suppress watch: `var dm = H.GetNodeOrNull<TargetDummy>(\"TargetDummy1\"); double before = dm?.Total ?? 0; var g = guest destroyer;` probe: when `g.Sl(\"grapnel\").Left > 0 && g.Sl(\"grapnel\").N == dm.NetId` note `hooked = true`; when hooked and `g.Sl(\"grapnel\").Left <= 0` record `took = dm.Total - before` and finish; assert `hooked && Math.Abs(took - 10) < 1e-6 && g.DealtBy.GetValueOrDefault(\"grapnel\") >= 10` ('arena host: the guest's grapnel cast off -- the dummy took 10 here, credited to the guest'). Await it before the freighter park as the suppress watch is.",
   "owner": "lane kits6a J9",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "cap-R7",
   "title": "LaneA6aBraceChecks run 2: the 30 u/s speed floor is a knife edge",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6aBraceChecks",
   "evidence": "tools/smoketest/SmokeTest.cs.txt:6572 `(run == 0 ? speed < 1 : speed > 30 && speed <= 44.5)`: a held battleship (top 44, thrust halved by Hold 0.5 on the whole helm, PlayerShip.cs:209-212) from rest reaches ~32-35 u/s at the 2.5-2.9 s sample (tau = top/thrust = 1.87 s), and run 2 holds D the whole time (:6555), which bleeds ahead speed through the keel: the floor sits on the value. The card's literal is the ceiling (x0.5 top = 44). FIX at :6572: `(run == 0 ? speed < 1 : speed > 15 && speed <= 44.5)` and the message at :6575 `({(run == 0 ? \"0\" : \"15-44\")})`.",
   "owner": "lane kits6a J2",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 1,
   "kept": false
  },
  {
   "id": "cap-R8",
   "title": "Ab.Gunships.Refuse reads the Hub's picks, so on the host it judges a guest's press by the host pilot's picks",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6aGunshipChecks refusal block (unchanged); a rung-5 stance-free carrier makes it harmless today",
   "evidence": "scripts/Abilities.cs:433 `(s.GetParent() as Hub)?.Targets` is the local pilot's pick list, not the ship's; DoAbility (PlayerShip.cs:670-671) invokes `def.Refuse` on the HOST for a GUEST's ship (the stance-end gate), where it reads the host pilot's picks. No wrong outcome today (the Carrier has no stance row; Launch :867 uses the press's own `_picked`), but the row's Refuse is not the ship's truth. FIX: make Refuse read the ship's own picks when it runs on the host: `var picks = s.Mine ? ((s.GetParent() as Hub)?.Targets.Where(t => t != null && t.Alive).ToList() ?? new List<IHittable>()) : s.Picked.ToList(); if (picks.Count == 0 && sel != null) picks.Add(sel);` replacing :433, the rest unchanged.",
   "owner": "lane kits6a J5",
   "files": "scripts/Abilities.cs",
   "unblocks": 1,
   "kept": false
  },
  {
   "id": "fr-R6b-1",
   "title": "Buster, Deploy and Well read the guest's AimPoint report, not the press's point (F8 rule)",
   "kind": "review",
   "rung": "six",
   "checks": "LaneA6bBusterChecks (add a wire pair LaneA6bBusterPointHost/Guest beside LaneA6bTotHost/Guest); LaneASentryThrowChecks (the guest R block ~22770 already holds the cursor still: add a second throw where the guest moves AimPoint the frame after R and asserts the host lands it on the PRESS spot within 10 u)",
   "evidence": "scripts/Abilities.cs:50-56 states the rule (\"a row reads where it was aimed from its slot -- never from a guest's AimPoint, which is a report behind the click\") and :801 (Slingshot) is the only row with TakesPoint = true. scripts/Abilities.cs:540-547 Buster has no TakesPoint and scripts/PlayerShip.cs:1107-1115 FireBuster aims at `AimPoint`; scripts/Abilities.cs:454-458 Deploy's Press is `s.DeployTurret(s.AimPoint)`; scripts/Abilities.cs:586-592 Well has no TakesPoint and scripts/PlayerShip.cs:1963-1968 Lay reads `AimPoint`. For a guest the host's AimPoint is the last 20 Hz intent, so a fast click-and-drag lands a buster, a sentry or a well where the cursor was up to 50 ms before the press (ledger_kits5 D31 deferred this to 6b; 6b did not do it). FIX: add `TakesPoint = true` to Ab.Buster, Ab.Deploy and Ab.Well; FireBuster: `var off = Sl(\"buster\").At - from;`; Deploy Press: `(s, _) => s.DeployTurret(s.Sl(\"deploy\").At)` (Refuse/Show keep AimPoint: courtesy only); Lay: `var aim = def.TakesPoint ? Sl(id).At : AimPoint;` and pass `aim` to Zones.Spot (the tether and curtain rows keep AimPoint). Checks: rung-5 host asserts the round/landing/zone within 10 u of the point the guest sent, the guest having moved AimPoint 300+ u the frame after the press.",
   "owner": "kits6b-J4/J5/J6 (Buster, Well) and kits5-J5 (Deploy, D31)",
   "files": "scripts/Abilities.cs, scripts/PlayerShip.cs, tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 3,
   "kept": true
  },
  {
   "id": "fr-R6b-2",
   "title": "A guest's TurretsOut is blind to throws in flight: its slot reads 3/3 and never ALL OUT for 0.8 s",
   "kind": "review",
   "rung": "six",
   "checks": "LaneASentryThrowChecks (host, unchanged); NEW guest lines in the arena guest block after its R throw (~22775): the frame after R, `fgm.TurretsOut == 1` and `Ab.Deploy.Show(fgm, null).Line == \"2/3\"` while `H.Deployed.Count == 0`",
   "evidence": "scripts/PlayerShip.cs:707 `_throws` is a private host list and :711-718 `TurretsOut` counts `_throws.Count + landed`; on a guest _throws is always empty, so scripts/Abilities.cs:466-470 Show prints `{max - TurretsOut}/{max}` = 3/3 during every 0.8 s flight and the 1.0 s redeploy flight, and Refuse at :461 never says ALL OUT with three in the air (invariant B: a visible state not reaching guests). FIX: on the host keep the count in the deploy slot: in DeployTurret (after `_throws.Add`), Redeploy (after the loop) and TickThrows (after `RemoveAt`) set `Sl(\"deploy\").N = _throws.Count`; TurretsOut becomes `int n = Net.Sim ? _throws.Count : Sl(\"deploy\").N; foreach landed n++`. Slots already ride the host report (D33), no new wire.",
   "owner": "kits5-J5 (throw), kits6b-J3 (redeploy)",
   "files": "scripts/PlayerShip.cs, tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "fr-R6b-3",
   "title": "The recall is an instant stow; the card says a 0.8 s flight back",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneASentryThrowChecks (the recall block ~5955-5963): after the recall assert `fr.TurretsOut == 3` at once and a throw refused ALL OUT, then `fr.TurretsOut == 2` 0.8 s (+-0.05, timed from the press) later and the stowed hull kept",
   "evidence": "docs/plans/cards/freighter.md Sentries: \"R within 60 u of one of yours recalls it (0.8 s flight back)\"; scripts/PlayerShip.cs:738-743 pushes the hull to `_stowed` and calls DeployedTaken the same frame (ledger_kits5 D33 took \"stows at once; 6b may add it\" as a default; 6b did not, and the card is the binding spec). FIX: add `private readonly List<(double Left, double Hp, double Max)> _home = new();` beside _throws; the recall branch does `_home.Add((Stats[\"deploy_flight\"], mine.Hp, mine.MaxHp)); MyHub?.DeployedTaken(mine);`; TickThrows also steps _home and on Left <= 0 pushes `(Hp, Max)` onto `_stowed`; TurretsOut adds `_home.Count` (and the slot N of R6b-2 carries `_throws.Count + _home.Count`). If the owner prefers the instant stow, the card line is what changes, not the code.",
   "owner": "kits5-J5 (D33)",
   "files": "scripts/PlayerShip.cs, tools/smoketest/SmokeTest.cs.txt, docs/plans/cards/freighter.md",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "fr-R6b-4",
   "title": "The lance beam a guest draws has no guest-role check",
   "kind": "review",
   "rung": "six",
   "checks": "LaneA6bFieldsHost / LaneA6bFieldsGuest (extend), or a new pair LaneA6bLanceHost/Guest beside them",
   "evidence": "ledger_kits6b D44: \"the slot carries it to every peer (Left while held, N 0/1 mend/2 burn, Own = length); drawn by a Fields row look Lance\"; scripts/Fx.cs:545 row `lance` and :652-661 draw from `sl.N` / `sl.Own`. tools/smoketest/SmokeTest.cs.txt:5794-5853 the only 6b rung-5 checks cover the paint, TOT lines and the two rings: nothing asserts a guest sees LanceBurn/LanceMend or the beam's length (CLAUDE.md 6.6: a guest-visible state needs a guest-role check). FIX: in LaneA6bFieldsHost, before the F/Q/E presses, spawn a held gunship 300-420 u from the tender on a varied bearing, hold Space with AimWorld on it for 2.5 s (the barrel at 90 deg/s) and assert `te.Sl(\"lance\").N == PlayerShip.LanceBurn` and `Math.Abs(te.Sl(\"lance\").Own - gap) < 25`; in LaneA6bFieldsGuest's LateSampler add `sawBeam |= te.Sl(\"lance\").N == PlayerShip.LanceBurn && Fields.Up(te).Any(f => f.Row.Id == \"lance\") && Math.Abs(te.Sl(\"lance\").Own - gunCopyDistance) < 25` and assert it.",
   "owner": "kits6b-J7",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 3,
   "kept": true
  },
  {
   "id": "fr-R6b-5",
   "title": "Sentries prefer the paint only within 650 u: the out-of-reach case has no check",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneASentryPreferChecks (add a fourth situation per run)",
   "evidence": "docs/plans/cards/freighter.md Sentries: \"Targets the painted target first whenever it is within 650 u; otherwise anything hostile\"; scripts/Turrets.cs:242-245 Preferred() requires `from.DistanceTo(p.Position) <= Range`. tools/smoketest/SmokeTest.cs.txt:4627-4628 paints a gunship 540-630 u from the sentry in every run: the reach gate is never exercised, so deleting `&& from.DistanceTo(p.Position) <= Range` would still pass. FIX: after the lapse check, spawn a second held gunship 700-780 u from the sentry (inside the freighter's 800), `pm.PaintOn(far, 5)`, 2 frames, assert `ReferenceEquals(sentry.Aimed, seeker)` (the nearer unpainted seeker keeps the gun) and `pm.Painted == far`; then move it to 560-620 u and assert the sentry turns to it within 2 frames.",
   "owner": "kits5-J4 (F14a)",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "fr-R6b-6",
   "title": "TOT 'allies never hit' is asserted on sentries that no line can reach",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6bTotChecks",
   "evidence": "tools/smoketest/SmokeTest.cs.txt:4758-4763 drops every own sentry on the FAR side of the gunship (`back = dir.Angle() + Pi`) and every TOT line stops at the target (AtTarget), so `mineWhole` at :4803-4806 is true whatever Lines.Strike's pool is; the card's \"Allies never hit\" (freighter.md ability 1) and D36's \"allies never\" rest on scripts/Lines.cs:96 `pool = Combat.Hostiles` with no check that would fail if the pool were Players too. FIX: per run drop one more own sentry ON the spotter's line at `home + dir * (tSpot.DistanceTo(home) * Vary(0.2f, 0.3f))` with hull 1e9 (it joins the guns: it is within 1500 u, so `want` lines become ks[run] + 2 -- adjust `onGun` and `credited` to `40 * (ks[run] + 2)` / `40 * (ks[run] + 3)`) and a battleship mate (LaneA6bMate, seat 5) held by LaneA6bHold at `home + dir * (tSpot.DistanceTo(home) * Vary(0.7f, 0.8f))`; assert the on-line sentry's Hp and the mate's Hp unchanged after the press.",
   "owner": "kits6b-J2",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "fr-R6b-7",
   "title": "Resupply 'never Resupply itself' on a second Tender in reach has no check",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6bResupplyChecks",
   "evidence": "docs/plans/cards/tender.md ability 3: \"never the drive, never Resupply itself\"; scripts/PlayerShip.cs:1003-1009 CoolingInAura and :1019-1027 Resupply skip `row.Id == id`, which is what protects ANOTHER tender's resupply cooldown too. tools/smoketest/SmokeTest.cs.txt:5745-5746 the mates are a sniper and a battleship: no tender, so the id-skip on another pilot is untested and the NOTHING COOLING refusal when only another tender's resupply cools is untested. FIX: add `var t2 = LaneA6bMate(ShipClass.FreightTender, 4, yonder + new Vector2(3000, 0));` held 300-480 u off in every run; before the press set `t2.Sl(\"resupply\").Cool = Vary(10f, 20f)` and `t2.Sl(\"overdrive\").Cool = Vary(10f, 20f)`; assert after the press `t2.Sl(\"resupply\").Cool` unchanged and `t2.Sl(\"overdrive\").Cool == od2 - 8`; in the nothing-cooling probe give only `t2.Sl(\"resupply\").Cool = 15` and assert `row.Refuse(td, null) == \"NOTHING COOLING\"`.",
   "owner": "kits6b-J8 (D45)",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "fr-R6b-8",
   "title": "Two card-named interactions have no check: Shockwave vs a latched webber, DD rip + Buster on a structure",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6bSiegeChecks (the Shockwave block ~5230-5252, and a new rip run after the pylon busts); or a new LaneA6bWebWaveChecks in solo after LaneA6bWellChecks",
   "evidence": "docs/plans/cards/bastion.md Interactions: \"the Bastion's own answer to a web is its Shockwave (it throws the webber off)\" and \"DD rip vs Bunker buster -- accepted, flagged: both add boss/structure damage\"; CLAUDE.md 6.2 wants a check per named pair. scripts/PlayerShip.cs:1096-1101 throws a Throwable 1000 u and scripts/Raider.cs:234 recomputes `Latched` from distance, so the webber should unlatch -- nothing asserts it, nor that the pilot's web status drops. No check lands `Fx.Tear`'s 1% + 10 rip and a 360 buster on the same pylon. FIX (a): after LaneA6bWellChecks' latched run pattern, a webifier latched on the bastion (`w.Quarry = bn`, wait for `w.Latched`), press shockwave, assert next frame `!w.Latched`, `w.Position.DistanceTo(bn.Position) > 900f`, and the bastion's web status (`bn.Statuses.Has(Status.Webbed)` or the tow/web flag the webber sets) is gone within 0.2 s; 3 varied bearings. FIX (b): in LaneA6bSiegeChecks' pylon loop, before the buster call `Dealt.Deal(p, 0.01 * p.MaxHp + 10, bs, \"grapnel\")` (the rip's door as the DD's grapnel lands it) then the buster; assert `took == 360 + 0.01 * p.MaxHp + 10` within 1e-6.",
   "owner": "kits6b-J5 (Buster, Shockwave hold)",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "fr-R6b-9",
   "title": "Time on target spends its cooldown when no gun is within 1500 u of the paint",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6bTotChecks (add: paint 1520-1600 u from the hull with no sentry out: refused OUT OF REACH, `blows.Count == 0`, `fr.Sl(\"tot\").Cool == 0`)",
   "evidence": "scripts/PlayerShip.cs:1056-1057 sets `Sl(\"tot\").Cool` before :1073 `if (from.DistanceTo(to) > reach) continue;` so a painted target that ran past 1500 u of every gun (800 u paint + 5 s at 260 u/s) costs the 16 s and lands nothing; scripts/Abilities.cs:479 Refuse knows only COOLING / NO PAINT. FIX: in TimeOnTarget move the cooldown after the loop under `if (n > 0)`; add to Refuse `: s.GunsInReach() == 0 ? \"OUT OF REACH\"` where `public int GunsInReach()` factors the muzzle list + reach test out of TimeOnTarget (TimeOnTarget calls it too, so the rule lives once).",
   "owner": "kits6b-J2 (D36)",
   "files": "scripts/PlayerShip.cs, scripts/Abilities.cs, tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 1,
   "kept": false
  },
  {
   "id": "fr-R6b-10",
   "title": "Deployed.cs header describes the old T-drop / C-collect path",
   "kind": "review",
   "rung": "solo",
   "checks": "quick (verify -Quick; no check changes)",
   "evidence": "scripts/Deployed.cs:5-6 \"The freighters drop these (T) and pick them up again (C, sitting over one)\": Ab.Collect and the T drop were deleted in kits5-J5 (R throws to the cursor, R within recall_pick recalls); invariant C (CLAUDE.md 9): no comment describes old behaviour. FIX: replace the sentence with \"A freighter throws one to its cursor (R, deploy_reach / deploy_flight) and recalls it with R within recall_pick of it (PlayerShip.DeployTurret); Redeploy folds every landed one onto a ring round the hull.\"",
   "owner": "kits5-J5",
   "files": "scripts/Deployed.cs",
   "unblocks": 1,
   "kept": false
  },
  {
   "id": "fr-R6b-11",
   "title": "Weak checks: no buster frame, the mortar's 2.35 s cadence sheet-only, Redeploy's cooldown at +-1.2 s",
   "kind": "review",
   "rung": "screens",
   "checks": "Shots.cs.txt LaneA6bMortarFrame (add frame 79f2_buster_round); LaneA6bMortarChecks; LaneA6bRedeployChecks",
   "evidence": "(a) scripts/Shots.cs:121 the Buster row (Look Ball, size 1.8) is drawn and tools/screens/Shots.cs.txt:1181-1194 has only the mortar circle: CLAUDE.md 6.5 wants a named frame for a drawn thing. (b) tools/smoketest/SmokeTest.cs.txt:4960-4966 proves 2.35 on the sheet only; the world runs (:4996-5000) hold Space until ONE mark and then release, so a Lob that reloaded every frame or never would pass. (c) :4936-4937 `Math.Abs(fr.Sl(\"redeploy\").Cool - fr.Cooling(20)) < 1.2` is a 6% window for a clock read in the same frame as the press (LaneA6bTotChecks uses 0.05). FIX: (a) in LaneA6bMortarFrame after the mortar snap: `bs.Sl(\"buster\").Cool = 0; bs.AimPoint = yard + new Vector2(0, -900); bs.UseAbility(\"buster\", 0); await W(0.9); await Snap(\"79f2_buster_round\");`. (b) in run 1 keep Space held until `H.BlastsPending == pending0 + 2` (cap 60*6 frames), record the clock of each mark and assert the gap is 2.35 +-0.06. (c) tighten to `< 0.05` (the press and the read are the same frame).",
   "owner": "kits6b-J3/J4/J5",
   "files": "tools/screens/Shots.cs.txt, tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 1,
   "kept": false
  },
  {
   "id": "hv-R6c-1",
   "title": "Warden PD is 10 DPS a mount; the ruling says 1",
   "kind": "review",
   "rung": "solo",
   "checks": "the Warden's SustainedDps check (SmokeTest.cs.txt:18340); LaneA6cFlakChecks row line (9064) unchanged",
   "evidence": "scripts/Ships.cs:542 sets [\"pd_damage\"] = 5.0 (every 0.5 s = 10 DPS a mount); Stats.cs:169-170 give every other class 0.5 / 0.5 s = 1 DPS, which SmokeTest.cs.txt:6668 asserts for the Battleship. docs/plans/README.md:63 (binding): \"Point defence passive, 1 DPS per mount, 2 on every capital\"; the Warden card's OPEN names this. FIX: Ships.cs:542 -> `[\"pd_count\"] = 1, [\"pd_range\"] = 420,` (the sheet's 0.5 stands) and delete the comment block Ships.cs:535-541 (\"half by MOUNTS ... 10 DPS\"); SmokeTest.cs.txt:18340 `+ 5 / 0.5` -> `+ 0.5 / 0.5`; cards/warden.md OPEN line -> built at 1 DPS. If the owner wants the 10 DPS, a ruling line in README.md instead and the code stands.",
   "owner": "kits6c (J11, Warden row) on a Ships.cs comment older than the lane",
   "files": "scripts/Ships.cs, tools/smoketest/SmokeTest.cs.txt, docs/plans/cards/warden.md",
   "unblocks": 4,
   "kept": true
  },
  {
   "id": "hv-R6c-2",
   "title": "Taunt: standoff heavies never treat the Warden as pinned nor boost in",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6cTauntChecks (new part 2: a standoff heavy)",
   "evidence": "cards/warden.md Taunt: \"standoff heavies boost in at once instead of waiting at range\"; ledger_kits6c D57 records it unbuilt (Known broken). scripts/Raider.cs:241 `bool pinned = t is IStatused st && st.Statuses.Has(Status.Pinned);` gates the heavy's missile (Raider.cs:262 `pinned || !Squad.Doctrine.MissileNeedsPin`, MissileNeedsPin defaults true, Squads.cs:51) so a called Gunship (Enemies.cs:94-98, Standoff, Missiles = true) never throws at a Warden that is not webbed; Squads.cs:309 `Burning = _sinceBurn >= Doctrine.Reboost` so a re-commit inside 10 s closes at cruise. FIX: Raider.cs:241 -> `bool pinned = (t is IStatused st && st.Statuses.Has(Status.Pinned)) || ReferenceEquals(Squad?.CalledBy, t);` and Squads.cs:159 (inside Call, before `if (!ReferenceEquals(Target, by)) { Target = by; Reform(); }`) add `_sinceBurn = Doctrine.Reboost;` so the commit the call forces burns. CHECK: in LaneA6cTauntChecks add, per run, a Gunship 600-900 u off at a VaryAngle bearing with Quarry = farS, its squad already committed (wait for Squad.Posted); after Q assert within 3.0 s of the guard's frame: a hostile missile is in flight at the Warden (InFlight().Any(m => m.HostileFire && m.TargetId == wd.NetId)) while wd.Pinned was never true, and the gunship's Squad.Burning was true on the frame it re-committed; remove D57 from CHANGES Known broken and DESIGN slice 6c.",
   "owner": "kits6c J12 / J15 (D57)",
   "files": "scripts/Raider.cs, scripts/Squads.cs, tools/smoketest/SmokeTest.cs.txt, docs/CHANGES.md, docs/DESIGN.md",
   "unblocks": 4,
   "kept": true
  },
  {
   "id": "hv-R6c-3",
   "title": "Whirlwind: a webifier still latches and lasers the spinning Warrior; the check asserts it",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6cWhirlChecks part 2 (SmokeTest.cs.txt:8066-8085)",
   "evidence": "cards/warrior.md Whirlwind: \"throws off every web on the hull and the Warrior cannot be latched while it spins\". Built (D47): only the Pinned status is refused (PlayerShip.cs:1464). scripts/Raider.cs:234 `Latched = Position.DistanceTo(post) < 12f && Gap(Position, t) <= Def.Reach && (Latched ? !_status.DropsLatch : !_status.BlocksLatch);` reads only the RAIDER's statuses, so the latch stays on and Raider.cs:248-251 fires its volley (Pins is true) every ShotEvery at the spinning hull. SmokeTest.cs.txt:8081 `if (t > 0.5) stillLatched &= web.Latched;` and :8084 assert the latch STILL ON, enshrining the gap. FIX: Raider.cs:234 append ` && !(Def.Cc == Status.Pinned && t is IStatused ut && ut.Statuses.Has(Status.Unwebbed))` (a pinner cannot latch a hull that refuses its pin). CHECK (fails on the old code first): :8073 `stillLatched` -> `unlatched`, :8081 -> `if (t > 0.5) unlatched &= !web.Latched;`, set `w.Hp = w.MaxHp` before the press and add `&& w.Hp == w.MaxHp` (the webifier's laser lands nothing through the spin) to :8084; message \"no web latches for the spin\"; keep the re-take after 2 s.",
   "owner": "kits6c J3 (D47)",
   "files": "scripts/Raider.cs, tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 3,
   "kept": true
  },
  {
   "id": "hv-R6c-4",
   "title": "Curtain: a body that leaves and re-enters takes every missed tick at once",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6cCurtainChecks (new part: out and back in)",
   "evidence": "scripts/Zones.cs:143-144: `Next[h]` is set once on the first touch and advanced by `next + z.Every` only while the body is inside; a raider that dips out for 1.5 s and comes back has `z.Age >= next` for three ticks, so it takes 10 on three consecutive frames (30 in 0.05 s). D58 says only \"keeps its clock (no second First)\", not a burst. FIX: Zones.cs:144 -> `else if (z.Age >= next - 1e-6) { z.Next[h] = Math.Max(next, z.Age) + z.Every; Dealt.Deal(h, z.TickHit, by, d.Id); }` (the remainder carries while inside; after a gap the clock restarts from now). CHECK: in LaneA6cCurtainChecks after the tick-k assertion (:9685): move inG to outAt for Vary(1.0, 1.5) s (Pin() with a swapped spot), then back to inAt; sample at Vary(0.1, 0.4) s after re-entry: took grew by exactly 0 or 10 (never 20+), and at Vary(0.6, 0.9) s by exactly 10 more than at re-entry; 3 runs. Same fix for any Field row (the well has none).",
   "owner": "kits6c J13 (D58)",
   "files": "scripts/Zones.cs, tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 3,
   "kept": true
  },
  {
   "id": "hv-R6c-5",
   "title": "Whirl() and Taunt() are hard-coded one-offs, not rows",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6cWhirlChecks, LaneA6cTauntChecks, LaneA6cTauntLatchChecks (rows unchanged; they must stay green)",
   "evidence": "scripts/PlayerShip.cs:2010-2017 Whirl() names \"whirlwind\", \"whirl_time\", \"whirl_cooldown\"; :1984-1995 Taunt() names \"taunt\", \"taunt_time\", \"taunt_reach\", \"taunt_cooldown\", \"taunt_guard\" and Fx.TauntRing -- beside StartDash(id)/Stance(id)/Lay(id)/Pop(id) which are generic (CLAUDE.md §7: the second of anything is a row). FIX: AbilityDef gains `string Time, Cooldown` (Time exists at Abilities.cs:178 for another spec; reuse) and `bool Sheds`; Whirlwind row: `Time = \"whirl_time\", Cooldown = \"whirl_cooldown\", Sheds = true, Press = (s, _) => s.Run(\"whirlwind\")`; PlayerShip.Run(string id): `if (sl.Left > 0 || sl.Cool > 0) return; sl.Left = Stats[def.Time]; sl.Cool = Cooling(Stats[def.Cooldown]); sl.Own = 0; if (def.Sheds) { LetGoWebs(); _status.Apply(Status.Unwebbed, sl.Left); }`. Taunt: `CallSpec { string Time, Reach, Guard, Cooldown; int Ring; }` on AbilityDef.Call, `Press = (s, _) => s.Call(\"taunt\")` with Taunt()'s body reading the spec's ids; delete Whirl() and Taunt(). No number moves; verify -Quick and the three checks prove it.",
   "owner": "kits6c J3, J12",
   "files": "scripts/Abilities.cs, scripts/PlayerShip.cs",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "hv-R6c-6",
   "title": "A DISABLED lunge stops the hull but the host keeps sweeping the 420 u line",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6cLungeChecks (new part 4: DISABLED inside the dash)",
   "evidence": "scripts/PlayerShip.cs:2117 `if (Disabled) { _dashLeft = 0; return false; }` ends the owner's carry, but the row's Left runs on (TickAbilities) and PlayerShip.cs:2104-2108 DashSweeps + Abilities.cs:705 `Expire = s => s.DashSweep(\"lunge\", 1)` strike every body out to the full Reach the hull never reached; the hardening runs the whole 0.3 s too. FIX: PlayerShip.cs:2107 -> `if (def.Dash is { } d && Sl(def.Id).Left > 0) { if (Disabled) { Sl(def.Id).Left = 0; _status.Clear(Status.Hardened); continue; } DashSweep(def.Id, 1 - Sl(def.Id).Left / Stats[d.Time]); }` (Disabled rides the wire, so the owner's carry and the host's line stop on the same state). CHECK: 3 runs, E then `w.ApplyStatus(Status.Disabled, 1.0)` at Vary(0.08, 0.18) s into the dash: the hull ends within 15 u of where it was on that frame, a gunship 330-400 u down the line takes 0 and one 40-90 u down it took 40 before; Left reads 0 within one frame.",
   "owner": "kits6c J2",
   "files": "scripts/PlayerShip.cs, tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "hv-R6c-7",
   "title": "The reload press's sender check has no check (G4 owed)",
   "kind": "review",
   "rung": "six",
   "checks": "NEW LaneA6cReloadGuest2Checks in the guest2 role (SmokeTest.cs.txt:23629), paired with LaneA6cReloadHostChecks",
   "evidence": "scripts/PlayerShip.cs:634-638 RequestReloadPress refuses a claim unless `Net.FromPlayer(this, out who) && who == OwnerId`; ledger_kits6c J6 records G4 (a third pilot's claim on the guest's ship refused) as owed. FIX (check only): in the guest2 role, once the guest's Sniper (H.Ships.First(s => !s.Mine && s.Class == HeavySniper)) shows Reloading, call `Net.AskHost(that, nameof(RequestReloadPress) via that.AskReloadPress(\"railgun\", 0.5f))` at 1.4-1.6 s of its reload and assert on guest2 that its slot N is never Perfect/Enhanced for that reload; the host watch adds a mutant-free proof: with the check's press landing the host's blow list (LaneA6cReloadSeen.Blows) still reads the five wanted figures. Rung 5 x2.",
   "owner": "kits6c J6 (owed G4)",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "hv-R6c-8",
   "title": "CurveParChecks' time-to-die loop is constant in L (a tautology)",
   "kind": "review",
   "rung": "solo",
   "checks": "CurveParChecks (SmokeTest.cs.txt:9965-9974)",
   "evidence": "SmokeTest.cs.txt:9970-9971: H = Par.DamageScale(L) * Rows[0].Hull, lancer = H / (15.76 * DamageScale(L) - 0.005 * H) = Rows[0].Hull / (15.76 - 0.005 * Rows[0].Hull) for every L: the three 'varied levels' vary nothing, and neither 15.76 nor 12.19 is tied to the Lancer's or Drake's rows. FIX: replace the loop with one assertion that names what it proves: `Check(Math.Abs(423.1 / (15.76 - 0.005 * 423.1) - 31.0) < 0.2 && Math.Abs(423.1 / (12.19 - 0.005 * 423.1) - 42.0) < 0.3 && Math.Abs(Par.Rows[0].Hull - 423.1) < 0.05, \"par's L1 hull 423.1 lives 31.0 s against the Lancer's cut sheet (15.76 DPS) and 42.0 s against the Drake's (12.19)\")`, and add the level-varied proof that IS in the table: at three Vary(2, 40) levels `Par.DamageScale(L) * Rows[0].Hull` equals the interpolated `Rows[L].Hull` within 1e-6.",
   "owner": "curve J1",
   "files": "tools/smoketest/SmokeTest.cs.txt",
   "unblocks": 1,
   "kept": false
  },
  {
   "id": "hv-R6c-9",
   "title": "The reload bar's miss state has no frame",
   "kind": "review",
   "rung": "screens",
   "checks": "LaneA6cRailFrames (Shots.cs.txt:1417-1436): new frame 73c2_sniper_reload_miss",
   "evidence": "scripts/ReloadBar.cs:78-83 dims the spot box (Dim) and flashes grey (MissC) after a MISSED press; Shots.cs.txt:1424-1427 only ever presses at 0.5 of the length (PERFECT) for 73c. FIX: after the 73c snap and the reload's end (:1428), fire again through `hold(); Abilities.Find(ShipClass.HeavySniper, \"railgun\").Loose(rs, 1.0);`, step until `rs.ReloadView.Since >= 0.2 * rs.ReloadView.Length`, `ActiveReload.Press(rs, ActiveReload.Rail)`, 3 frames, `await Snap(\"73c2_sniper_reload_miss\")` (the grey flash, the dimmed box), then let it seat before 73d; add the name to the frame manifest the screens runner reads.",
   "owner": "kits6c J7",
   "files": "tools/screens/Shots.cs.txt",
   "unblocks": 1,
   "kept": false
  },
  {
   "id": "lt-R6d-1",
   "title": "Slingshot/Step: host skip-yaw is a one-shot flag racing the report; no guest check for the slung Ramjet",
   "kind": "review",
   "rung": "six",
   "checks": "NEW LaneA6dSlingHostWatch / LaneA6dSlingHostChecks / LaneA6dSlingGuestChecks (rung 5); LaneA6dSlingChecks unchanged",
   "evidence": "C:\\Users\\logan\\Downloads\\WarShips_Version_L\\scripts\\PlayerShip.cs:923 `if (SkipYaw) { yaw = 0f; SkipYaw = false; }` and :938 `public bool SkipYaw;`, set at :960 (Slung) and :1308 (Stepped). The guest snaps at once and its next unreliable state report already carries the new heading; the reliable RPC that sets SkipYaw may land before or after that report, so the flag either skips the pre-snap report (the snap then bleeds the host's ramp copy by bleed x min(1,|yaw|/turn) x age, ~1% at 20 Hz, 10% at a 0.5 s gap) or skips a later real turn. The card (dart.md Interactions: 'a snap, not a turn, so it never bleeds a lit Ramjet'; DL5 'the host's copy skips the report') names this host-side interaction, and LaneA6dSlingChecks (SmokeTest.cs.txt:11156) proves it solo only, where Mine steps from _yawRate and SkipYaw is never read. FIX (code): replace :938 with `private double _skipYawUntil; public void SkipYawFor(double secs) => _skipYawUntil = _clock + secs;`, replace :923 with `if (_clock < _skipYawUntil) yaw = 0f;`, replace :960 and :1308 `if (!Mine) SkipYaw = true;` with `if (!Mine) SkipYawFor(0.4);` (two round trips; comments at :919 and :1301 and Abilities.cs:798 name SkipYawFor). FIX (check, rung 5, beside ramjetWatch at :22384 / after LaneA6dRamjetHostChecks / after LaneA6dRamjetGuestChecks): guest Dart flat out on VaryAngle lights the ramjet; host watch samples its copy's Sl(\"ramjet\").Own once Left <= ramjet_time-4.5 (expect >= 0.44) then, on seeing Sl(\"slingshot\").Cool > 0, samples Own 0.6 s later: assert own_after >= own_before - 0.005; guest: after 4.5 s lit, AimPoint = Position + Up.Rotated(Rotation + DegToRad(Vary(120,170)) * sign) * 500, UseAbility(\"slingshot\",0), assert its own Own kept and Rotation on the cursor.",
   "owner": "kits6d J4 (Slingshot) / J11 (Step)",
   "files": "C:\\Users\\logan\\Downloads\\WarShips_Version_L\\scripts\\PlayerShip.cs; C:\\Users\\logan\\Downloads\\WarShips_Version_L\\scripts\\Abilities.cs; C:\\Users\\logan\\Downloads\\WarShips_Version_L\\tools\\smoketest\\SmokeTest.cs.txt",
   "unblocks": 3,
   "kept": true
  },
  {
   "id": "lt-R6d-2",
   "title": "Slipstream: no boost interaction check and no guest-role check (the host judges a guest from its report)",
   "kind": "review",
   "rung": "six",
   "checks": "LaneA6dSlipChecks (extend); NEW LaneA6dSlipHostWatch / LaneA6dSlipHostChecks / LaneA6dSlipGuestChecks (rung 5)",
   "evidence": "SmokeTest.cs.txt:11228-11276 LaneA6dSlipChecks sets dt.Velocity by hand for every situation; nothing presses V. dart.md Interactions: 'Boost pushes a Dart above 325 u/s (Slipstream) within 0.4 s; the host's clamp uses hypot(top, strafe) x 1.1' (§3.4:281). PlayerShip.cs:966-974 SlipShare reads Velocity when Mine but SlipSpeed(_netVel, TopNow, StrafeNow) for a guest's copy, i.e. host-decided from the report and its lifts: a guest-visible state with no rung-5 check (CLAUDE.md 6.6). FIX (rung 3, append to LaneA6dSlipChecks): 3 runs at VaryAngle: Dart flat out (KeyDown W until SpeedAhead >= 259), Hit(10, side, source) lands 10 (x1); Key(V); from the first frame Sl(\"boost\").Left > 0 count frames until Velocity.Length() >= 325 (assert <= 0.4 s), then Hit(10, ...) lands 7.0 (x0.7). FIX (rung 5, after the rod pair): host watch waits for the guest Dart's Sl(\"boost\").Left > 0 and its clamped _netVel (g.Velocity) >= 325, then `g.Hit(10, g.Position + Vector2.Right * 60, \"test:6d_slip5#1\")`: hull drops 7.0 +-1e-6; earlier, at rest, the same blow drops 10; guest: presses V flat out on VaryAngle and asserts its own hull fell by 7 within 1 s of the host's blow (MaxHp 200 first, as LaneA6dPepperGuestChecks waits).",
   "owner": "kits6d J4 (Slipstream)",
   "files": "C:\\Users\\logan\\Downloads\\WarShips_Version_L\\tools\\smoketest\\SmokeTest.cs.txt",
   "unblocks": 3,
   "kept": true
  },
  {
   "id": "lt-R6d-3",
   "title": "Ruling R6 (every top-speed lift prices the Dart, gear included) has no check",
   "kind": "review",
   "rung": "solo",
   "checks": "NEW LaneA6dPepperGearChecks (rung 3, after LaneA6dRodRecoilChecks; call after `await Lane(\"LaneA6dRodRecoilChecks\", ...)` ~18680)",
   "evidence": "numbers_curve_raids_items.md §8 R6: 'the Dart 72.0: every top-speed lift prices the Pepperbox and the rod, the boost and gear included; the items pass re-checks it'. ledger_items.md reconcile audit: 'A9 HOLDS ... the Dart prices its live top (TopNow)' — asserted by reading, not by a check. Bores.cs:36-40 prices from s.TopNow = TopSpeed(Stats[\"max_speed\"], SpeedMult, SpeedAdds(), 1) so a gear row on max_speed or ~surge_lift reaches the price, but LaneA6dPepperChecks (SmokeTest.cs.txt:10771-10788) proves only rest (7.5) and the boost (11.25) and ItemsBoostLiftChecks (:13750) proves only the lift, never a dart or rod. FIX: 3 situations through WearI/WornOffI on ShipClass.LightDart: (a) chip_combat_t1 (Items.cs:293-294, Down max_speed Price.ChipHelm -0.04): TopNow 249.6, LaneA6dOneDart damage 7.5 (the x1 floor) and, with Sl(boost).Left 1.5 and Sl(\"rod\").Left 0.02, the rod 180 x (249.6 x 1.5 + 100) / 260 = 328.43 +-0.05; (b) burner_drive_t1 (Items.cs:287-289, ~surge_lift +25% of its excess = x1.625) boosted: TopNow 422.5, a dart 11.25, the rod 360 (cap, 522.5); (c) hauler_drive is Freighter-only, so the third is chip_combat_t10 boosted sprint: compute max_speed from `new ShipStats(c, Equipment.Bonuses(c, lo, 14, NoLevels))` as ItemsReconcileChecks.On does and assert the rod equals Bores.Price(180, top x 1.5 + 100, 260, 2) AND that top < 249.6 (the chip's tiered price bit). Each at VaryNear/VaryAngle.",
   "owner": "items reconcile (A9) / kits6d J1-J2",
   "files": "C:\\Users\\logan\\Downloads\\WarShips_Version_L\\tools\\smoketest\\SmokeTest.cs.txt",
   "unblocks": 3,
   "kept": true
  },
  {
   "id": "lt-R6d-4",
   "title": "Rod from God under a web: the planned 'webbed' check was dropped; the card's no-web-breaker interaction is unchecked",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6dRodChecks (extend with a webbed block, 3 runs)",
   "evidence": "ledger_kits6d.md:118-122 (J2 plan) lists 'webbed: down the frozen heading'; the J2 POST (:240-244) lists no such check and SmokeTest.cs.txt:10779-10913 has no Pinned/webbed block (grep confirms). dart.md Interactions: 'The Dart has no web-breaker at all — only hull ... boost never breaks a web'; LaneBBoostChecks (:3592-3601) proves it for the boost (78 u/s), nothing proves it for the Forced sprint (PlayerShip.cs Forced -> throttle 1; Held x0.2 after the lifts, D6). The rod is priced at TopOf(def) (PlayerShip.cs:894, hold 1f) so a webbed sprint's rod still carries 249.2. FIX: 3 runs at VaryAngle from rest: `dt.Statuses.Apply(Status.Pinned, 6.0)`, await Frame, UseAbility(\"rod\",0); over the run assert dt.Pinned stays true, Velocity.Length() <= 0.2 x 360 + 3 every frame, |AngleDifference(rot, Rotation)| < 1e-3 (the web freezes the heading), the rod leaves at 3.0 s +-1.5 frames with Dir within 1e-3 rad of the frozen nose and Damage == Bores.Price(180, 360, 260, 2) (249.23 +-0.01); then Statuses.Clear(Pinned).",
   "owner": "kits6d J2",
   "files": "C:\\Users\\logan\\Downloads\\WarShips_Version_L\\tools\\smoketest\\SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "lt-R6d-5",
   "title": "EMP: 'standoff heavies head back to the edge' is on the card with no check",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6dEmpChecks (extend, 3 runs)",
   "evidence": "echo.md EMP: 'a web on a friend lets go; standoff heavies head back to the edge'. ledger_kits6d.md DL4: 'standoff heavies leave a non-pinned target for the edge (by construction once the web drops -- check it)'. SmokeTest.cs.txt:11760-11794 proves the webs drop, 0 struck, no launch and the turret held, never the heavy's retreat. Raider.cs:31 `Heavy => Def.Way == EnemyWay.Standoff`; :293 the edge point is Hub.BasePos + dir * Hub.RaidEdge. FIX: 3 runs: Echo at VaryNear(yonder,300); one webifier latched on it (as :11768-11772) and one heavy (Enemies.Gunship, Heavy true) held 200-280 u off at VaryAngle with Quarry = me; wait until latched; UseAbility(\"emp\",0); record d0 = heavy.Position.DistanceTo(me.Position) on the frame the web drops (freedAt) and assert over the next 3.0 s that the heavy's distance to the Echo grows by >= 100 u AND its distance to the edge point (Hub.BasePos + (me.Position - Hub.BasePos).Normalized() * Hub.RaidEdge) shrinks, the Echo held still; then kill both.",
   "owner": "kits6d J7",
   "files": "C:\\Users\\logan\\Downloads\\WarShips_Version_L\\tools\\smoketest\\SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "lt-R6d-6",
   "title": "Items reconcile: Ace Core's lifts of rod_damage and venom_dps have no literal and no live check",
   "kind": "review",
   "rung": "solo",
   "checks": "ItemsReconcileChecks (add two Want lines); NEW ItemsVenomLiftChecks (rung 3, after ItemsAnchorLiftChecks)",
   "evidence": "Items.cs:106-108 @output gained rod_damage and venom_dps (R-D2, ledger_items.md:311-313); SmokeTest.cs.txt:13935-13977 asserts Ace Core only on rod_range 1008 / step_reach 648 / emp_range 216 (the @area price), never the output it buys, and ItemsAnchorLiftChecks (:13983) is the only live item-x-ability interaction. Doses.Tick (Doses.cs:52) reads st[o.Def.Rate] live, so a lifted venom_dps changes every tick: 10 x 1.5625 x 0.5 = 7.8125 at T1. FIX: in ItemsReconcileChecks add `Want(ShipClass.LightDart, \"ace_core_t1\", \"rod_damage\", 225); Want(ShipClass.LightWraith, \"ace_core_t1\", \"venom_dps\", 1.5625, 1e-9); Want(ShipClass.LightWraith, \"ace_core_t10\", \"venom_dps\", 1.25 * (1 + 0.25 * Tiers.P(10)), 1e-6);` and extend its message. NEW ItemsVenomLiftChecks: 3 situations (no part / ace_core_t1 / ace_core_t10) via WearI on LightWraith at VaryNear/VaryAngle: coat, hold the trigger on a held gunship 60-100 u off until DosedOn == 10 (as LaneA6dVenomChecks :12200-12215), then assert each tick of DealtBy[\"venom\"] equals 6.25 / 7.8125 / 10 x 1.25 x (1 + 0.25 x Tiers.P(10)) x 0.5 (+-1e-6), timed from the first tick.",
   "owner": "items reconcile J1",
   "files": "C:\\Users\\logan\\Downloads\\WarShips_Version_L\\tools\\smoketest\\SmokeTest.cs.txt",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "lt-R6d-7",
   "title": "Stale learn-order literals in the Echo's Repeater and Rewind row checks (deterministic red)",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneA6dRepeaterRowChecks, LaneA6dRewindRowChecks",
   "evidence": "SmokeTest.cs.txt:11270 `kit.SequenceEqual(new[] { \"guns\", \"firemode\", \"reverb\" })` and :11484 `kit.SequenceEqual(new[] { \"guns\", \"firemode\", \"reverb\", \"rewind\" })` against the FULL Classes.Of(LightEcho).Abilities id array, which Ships.cs:684 fills with five (guns, firemode, reverb, rewind, emp); SequenceEqual needs equal length, so both read FAIL on every seed (echo.md OPEN states it; it may already be one of the 42 solo reds). LaneA6dEmpRowChecks :11700 already asserts the five. FIX: at :11270 and :11484 replace the array with `new[] { \"guns\", \"firemode\", \"reverb\", \"rewind\", \"emp\" }` and change the messages' 'learns'/'learned second' text to name the five; do not weaken to Take(n).",
   "owner": "kits6d J5 / J6",
   "files": "C:\\Users\\logan\\Downloads\\WarShips_Version_L\\tools\\smoketest\\SmokeTest.cs.txt",
   "unblocks": 1,
   "kept": false
  },
  {
   "id": "net-RV1",
   "title": "Rejoin token: add the wire check that a peer WITHOUT the token is refused a held/live place; un-tautologise the host's P10b line",
   "kind": "review",
   "rung": "six",
   "checks": "ArenaMp host line \"arena host: the guest back under a new id claimed its place with the rejoin token this host issued it (P10b)\" (SmokeTest.cs.txt:22662-22663); R2cHostReturns; S2TokenChecks; NEW S2TokenWireHost/S2TokenWireThird",
   "evidence": "Hub.cs:857-863 is the only authority gate on a held place (`Session.MayClaim(characterId, token ?? \"\", holder != 0)`; a failing claim sets characterId=\"\"; a passing one with a live holder hangs it). No check anywhere sends an identity with a WRONG or EMPTY token for an id that is held or live (grep MayClaim: only the pure S2TokenChecks at 21629-21645). The host's positive line at 22662 asserts only `Session.Tokens.ContainsKey(agInfo.CharacterId)` -- true the moment the host issued a token at the FIRST join, so it passes even if the claim were taken by name (the code path the rule replaced). FIX (a) 22662: also assert `agInfo.Token == Session.Tokens[agInfo.CharacterId]` (the identity that took the place carried the issued token) and `agInfo.Token.Length == 32`. FIX (b) new negative wire check in the third-player role (guest2, after Guesty's drop and BEFORE its return): build Third's own identity args exactly as Hub.SendIdentity does (`Net.LocalId, Character.Name, Character.Main, Character.Accent, (int)Character.Class, Character.Bought, Character.Level, Character.Peak, Character.LoadoutFor(Character.Class), Character.GearLevel.Keys.ToArray(), Character.GearLevel.Values.ToArray(), <Guesty's character id as the role builds it, the string R2cHostReturns reads with EndsWith(\"-guest\")>, \"\"`), send `H.RpcId(1, \"NetIdentity\", args)`, then `H.PilotChanged()` to restore its own; host half (`S2TokenWireHost`, 1 s later): `Session.Places.ContainsKey(guestCid)` still true, `Net.I.Players[thirdId].CharacterId` is Third's own id (never guestCid), Third's ship still in the party under its own id. Repeat once while Guesty is LIVE (before the drop) with token \"deadbeef\"x4: 1 s later `Multiplayer.GetPeers().Contains(guestyId)` and `_replacing` (reflection) is empty. Both must fail if line 862's `!Session.MayClaim(...)` is replaced by `false`.",
   "owner": "lane net2 (S2b, R2c); gate fix (GateFixTokenWatch)",
   "files": "tools/smoketest/SmokeTest.cs.txt (ArenaMp host + guest2 roles, ~22640-22670 and the guest2 block ~11222+); scripts/Hub.cs:857-863 (read-only reference)",
   "unblocks": 4,
   "kept": true
  },
  {
   "id": "net-RV2",
   "title": "Watchdog: a peer that completes the handshake but never answers the welcome is never beaten nor dropped (holds a Players slot, counts toward Full)",
   "kind": "review",
   "rung": "six",
   "checks": "R2BeatChecks (solo rule); NEW R2WelcomeWatchdogHost (host role) + a guest2 `Net.Pretend.Mute` leg",
   "evidence": "Net.cs:391-410 `Beat`: `var peers = _isHost ? _heard.ToList() : ...` -- the host beats and times out ONLY peers already in `_heard`; `_heard.Add(from)` happens only in NetWelcomed (Net.cs:364-369). OnPeer(joined) at 1047-1056 adds the peer to `Players` at once, so `Full` (Net.cs:621) counts it forever; nothing else has a deadline (only `_times[..].Admitted`, a report figure). A modified or stalled client that finishes SendAuth and never sends NetWelcomed holds a place until the transport itself closes. FIX Net.cs:394: `var peers = _isHost ? Players.Keys.Where(p => p != 1).ToList() : new List<int> { 1 };` keep the send loop (`RpcId(p, nameof(NetBeat), ...)`) on `_heard` only (`foreach (int p in peers.Where(p => !_isHost || _heard.Contains(p)))`), leave the overdue loop on `peers` (a never-welcomed peer's silence grows from its join and is hung up after Link.QuietMs); in NetWelcomed add `_silence[from] = 0;`. CHECK: add `Mute = 4` to `Net.Pretend` (Net.cs:197) and in NetWelcome (Net.cs:345-352) skip `RpcId(1, nameof(NetWelcomed))` when `PretendAt.HasFlag(Pretend.Mute)` (the same seam pattern as Code/Auth); guest2 role: before its real join, join once with `Net.PretendAt = Net.Pretend.Mute` and assert it is let go by the host 8 +/- 1 s after `Connected as player` (timed from the frame `Net.IsOnline` first reads true, not from the JOIN press); host role: `Net.I.Players.Count` back to its former value within 9.5 s of the join event and `Net.I.Full == false` throughout. Solo: extend R2BeatChecks with the pure rule via reflection on `Beat`'s peer set is not possible -- rely on the rung-5 pair.",
   "owner": "lane net2 (R2a/R2b)",
   "files": "scripts/Net.cs:197,345-352,364-369,391-410; tools/smoketest/SmokeTest.cs.txt (R2BeatChecks 21660, Mp guest2 role ~11222, host role)",
   "unblocks": 3,
   "kept": true
  },
  {
   "id": "net-RV3",
   "title": "Dealt.Deal credits, echoes and announces a blow on a target that was already dead",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneADamageDoorChecks (2776); the echo/reverb and venom OnDealt checks that read DealtBy / Sl(\"reverb\").Own",
   "evidence": "Dealt.cs:34-41: `bool was = target.Alive; ... target.TakeDamage(d); by?.NoteDealt(d, target, weapon); ... Landed?.Invoke(target, d, weapon);` -- Raider.TakeDamage (Raider.cs:161) and Boss.TakeDamage (Boss.cs:243) return at once when `!Alive`, so on a corpse nothing lands, yet PlayerShip.DealtBy grows by d (PlayerShip.cs:327), the Echo's reverb stores it (`Abilities.cs:817 e.Own += d`), the Venom doses it (856), and Landed reports it. The card/ledger truth is `DealtBy[weapon] == hull lost` (ledger_kits D17). FIX Dealt.cs:36 insert after `bool was = target.Alive;`: `if (!was) return;` (a blow on a corpse is nothing: no credit, no OnDealt, no Landed). CHECK (fails on the old code first): in LaneADamageDoorChecks' BB block, after `mainTgt.TakeDamage(1e12); await ProcessFrame;` add `double before = bs.DealtBy.GetValueOrDefault(\"shell\"); Dealt.Deal(mainTgt, 50, bs, \"shell\"); Check(Math.Abs(bs.DealtBy.GetValueOrDefault(\"shell\") - before) < 1e-9, \"a blow on a target already dead credits nothing (D17)\");` inside the existing 3-run loop (3 varied spots).",
   "owner": "lane kits slice 2 (J5, F4)",
   "files": "scripts/Dealt.cs:34-41; tools/smoketest/SmokeTest.cs.txt:2776-2815",
   "unblocks": 3,
   "kept": true
  },
  {
   "id": "net-RV4",
   "title": "Turrets picks the damage-door weapon id by a type test (`Host is PlayerShip`) instead of a row field",
   "kind": "review",
   "rung": "solo",
   "checks": "LaneADamageDoorChecks PD block (\"DealtBy[\\\"pd\\\"] grew\") and freighter turret block (\"DealtBy[\\\"turret\\\"] on its OWNER\") -- unchanged literals keep proving it",
   "evidence": "Turrets.cs:208 `Dealt.Deal(tgt, spec.Damage, Host, Host is PlayerShip ? Dealt.Pd : Dealt.Turret);` -- CLAUDE.md 7 and gate.md forbid a type test where a row should be; a third ITurretHost (an emplacement, a wing) would be credited as \"turret\" by accident. FIX: TurretSpec (Turrets.cs:31-38) gains `public string Weapon;` (\"the weapon id the door credits; null: the round's own row, Shots.Of(Kind).Id\"); PlayerShip.Spec(pd: true) sets `Weapon = Dealt.Pd`, DeployedTurret.Spec sets `Weapon = Dealt.Turret` (Deployed.cs, the Spec override), Emplacements/other hosts leave it null; Turrets.cs:208 becomes `Dealt.Deal(tgt, spec.Damage, Host, spec.Weapon ?? Shots.Of(spec.Kind).Id);`. TurretSpec is a StructRow, so the new field is hashed by Net.Fingerprint with no further work. No new check: the two existing literal blocks fail if either host names the wrong id.",
   "owner": "lane kits slice 2 (J5, F4)",
   "files": "scripts/Turrets.cs:31-38,208; scripts/PlayerShip.cs (Spec(bool pd)); scripts/Deployed.cs (Spec)",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "net-RV5",
   "title": "Session.Rejoin is one token for every host, not per host as the ledger records; a return to host A after a visit to host B is refused its held place",
   "kind": "review",
   "rung": "solo",
   "checks": "S2TokenChecks (21629); R2cGuestReturns' `Session.Rejoin.Length == 32`",
   "evidence": "Session.cs:65-72: `public static string Rejoin = \"\";` set by Hub.NetToken (Hub.cs:879) from whichever host last issued one, sent to peer 1 by Hub.SendIdentity (Hub.cs:831) whatever host that is. ledger_webrtc S2b POST says \"the guest keeps it per host\". Sequence: join A (token tA), drop, join B inside A's 90 s hold (B issues tB, Rejoin = tB), come back to A: MayClaim(id, tB, ...) -> refused, characterId = \"\", the held place and its owed kill are lost. FIX Session.cs: `[Live] public static readonly Dictionary<string, string> Rejoins = new();` + `public static string RejoinFor(string host) => Rejoins.GetValueOrDefault(host ?? \"\", \"\");` + `public static void Remember(string host, string token) { if (!string.IsNullOrEmpty(host)) Rejoins[host] = token ?? \"\"; }`; Hub.cs:879 `NetToken`: `if (!Net.IsHost) Session.Remember(Net.I.LastHost, token);`; Hub.cs:831: `args[^1] = to == 1 && !Net.IsHost ? Session.RejoinFor(Net.I.LastHost) : \"\";` (LastHost is the join text, set in OnConnected, Net.cs:330; a paste invite's text differs per invite -- key on the host's `Name` from the invite instead: expose `Net.I.HostName` set from `invite.Name`/`_joinTarget` in OnConnected and key on that). Delete `Rejoin`; R2cGuestReturns reads `Session.RejoinFor(Net.I.HostName).Length == 32`. CHECK in S2TokenChecks (3 varied host names): Remember(h1,ta); Remember(h2,tb); RejoinFor(h1)==ta && RejoinFor(h2)==tb && RejoinFor(\"nobody\")==\"\"; End() keeps them.",
   "owner": "lane net2 (S2b)",
   "files": "scripts/Session.cs:65-72,44; scripts/Hub.cs:819-833,877-879; scripts/Net.cs:326-334; tools/smoketest/SmokeTest.cs.txt:21629-21645,22262-22275",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "net-RV6",
   "title": "A class change the host refuses (arena / in combat) is not replicated: other guests take the announced class under their own InArena",
   "kind": "review",
   "rung": "six",
   "checks": "S2Checks P8 lines (21617-21624, pure); GateFixRefitChecks; NEW GateFixRefitWire (guest2 role)",
   "evidence": "Hub.cs:864-866 every peer runs `Refit(p.HasIdentity ? p.Class : null, Classes.Sanitize(cls), InArena, cship?.InCombat == true)` on the identity it hears; `InArena` is this peer's own sector, so a guest at home hearing a pilot in the arena announce a new class TAKES it while the host keeps the old hull -- a visible state (the hull every peer draws and aims at) that does not reach guests from the host (invariant B). FIX: on the host, after `var klass = Refit(...)`, if `klass != Classes.Sanitize(cls)` send `Rpc(nameof(NetClassKept), peer, (int)klass)`; add `[Rpc(Authority, CallLocal = false, Reliable)] private void NetClassKept(int peer, int cls) { if (Net.IsHost || !Net.I.Players.TryGetValue(peer, out var p)) return; p.Class = (ShipClass)cls; if (_ships.TryGetValue(peer, out var s) && IsInstanceValid(s)) ApplyIdentity(s); }`; on non-host peers Hub.cs:865 keeps its local Refit (fast path) -- the host's word overrides it a packet later. CHECK guest2 role, in the arena with Guesty present: Guesty's role does `Character.Class = <another Ready class>; H.PilotChanged();` (the UI gate MayRefit bypassed on purpose), then restores; guest2 asserts within 1.5 s `Net.I.Players[guestyId].Class` equals the class the HOST holds (host posts it through the courier, or assert it equals the class Guesty had before the announcement, read 1 s earlier -- a stale-pick-safe re-read after the wait). Fails on the old code (guest2 shows the new class).",
   "owner": "lane net2 (S2a P8, gate fix 2)",
   "files": "scripts/Hub.cs:836-876,889-890; tools/smoketest/SmokeTest.cs.txt (ArenaMp guest/guest2 roles)",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "net-RV7",
   "title": "Net._ExitTree closes the session peer but leaves every pending invite's WebRtcPeerConnection and the guest's answer gather open (invariant A)",
   "kind": "review",
   "rung": "quick",
   "checks": "the run-end orphan/object-count line; R2SwitchChecks \"back offline: every invite hung up\" (covers Shutdown, not _ExitTree)",
   "evidence": "Net.cs:132-148 `_ExitTree`: rows closed, `_lettingGo` closed, `_peer?.Close()` -- but Pending entries' `e.Conn` (Link.Gather, each holding a live WebRtcPeerConnection added under `_peer`, Link.cs:167-176) and `_answer` are never `Close()`d; Shutdown (Net.cs:1018-1027) does hang them, and DESIGN.md's trap says a WebRTC handle released by the collector's thread turns into an engine ERROR. Game.Quit reaches Shutdown via Close(), but a scene-tree teardown that removes the autoload without Quit (the harness's role end, an editor stop) does not. FIX: extract from Shutdown lines 1021-1025 a `private void HangAll() { foreach (var e in Pending.All.ToList()) Hang(e.Id); foreach (var m in _making) m.done.TrySetResult(null); _making.Clear(); if (_answering is var (_, answered)) answered.TrySetCanceled(); _answering = null; _answer?.Close(); _answer = null; }`, call it from Shutdown in place of those lines and from _ExitTree right after the rows are closed (before `_peer?.Close()`). Note `Hang(id)` for a Pending entry is synchronous (`e.Conn.Close()`, Net.cs:207-211), so it is safe in _ExitTree. No new check: the orphan-count line at rung 2/3 is the proof; add the harness's HermeticNetwork host with 2 invites pending at the solo run's end so the line has something to see.",
   "owner": "lane net2 (R2b)",
   "files": "scripts/Net.cs:132-148,1018-1027; tools/smoketest/SmokeTest.cs.txt (solo end)",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "net-RV8",
   "title": "RaidsSquadChecks N2 lacks the spec's third situation (the target boosting away: the squad never splits, re-commits and pins once it stops)",
   "kind": "review",
   "rung": "solo",
   "checks": "RaidsSquadChecks N1+N2 block (SmokeTest.cs.txt:14833-14880)",
   "evidence": "raids_squads_adds.md:363 N2: \"target still / moving / boosting -- one of the three per seed ... boosting away: the squad never splits, and re-commits and pins once the target stops\". SmokeTest.cs.txt:14834 `bool moving = Vary(0f, 1f) < 0.5f;` draws only still/moving; nothing anywhere runs the boosting case, which is the one that exercises Squad.Leash (Squads.cs:222 `Raider.Gap(Centre, Target) > Doctrine.Leash * CommitAt -> Reform`) and Reboost (Squads.cs:283-284). FIX: replace line 14834 with `int how = Rng.RandiRange(0, 2); bool moving = how == 1, boosting = how == 2;` and in `Step()` for `boosting`: from the first frame any member's boost bit (FlagBoost) is up, run the pilot at `me.Stats[\"top_speed\"] * 1.5f` straight away from `sq.Centre` for `Vary(4f, 7f)` s (timed from that frame, not the spawn), then stop; assert: `sq.Members.Count == 4` on every frame (never splits), `sq.Phase` reads Approach at least once after the run (re-formed, from the Leash), a second commit (`sq.Phase == Commit` again) and `me.Pinned` within 6 s of the stop -- this frame's `sq`/`ms` re-read after every wait. Keep the existing still/moving asserts for how 0/1; the label in the message names the case.",
   "owner": "lane raids (J2)",
   "files": "tools/smoketest/SmokeTest.cs.txt:14830-14882",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "net-RV9",
   "title": "The listener's Full refusal (Why.Full) is never exercised on the wire; only the paste row's FullText is checked",
   "kind": "review",
   "rung": "six",
   "checks": "R2SwitchChecks \"the session is full at 8 with invites counted\" (21790-21797, paste row only); Mp guest2 role (\"its knock as another build is refused by the listener before any connection\"); NEW R2FullKnock host/guest2 lines",
   "evidence": "Rendezvous.cs:683-694 Weigh: build, then rate, then `if (_desk.Full) why = Why.Full;` -> `_desk.Refused(knock, Full)` -> Net.cs:702 `\"{who} knocked, but the session is full (8 players, counting invites not yet answered).\"`; the guest side maps a Refuse record to text in Net.Connect/Joining (Net.cs:922-1000). Neither literal is asserted anywhere; the address row's refusal chain has a check only for Why.Build (guest2, PretendAt.Code). FIX: in the Mp host role, before guest2's typed-address join, `Net.I.Invite()` until `Net.I.Full` (as R2SwitchChecks does, each awaited on `Clip.Length > 0`), post \"full\" through the courier (`Post(CourierFile(\"full\", P(27115), 0), \"1\")`); guest2 waits for that file, then `Net.I.Join(address)` and asserts within 5 s `Net.I.LastStatus` == the guest's Why.Full literal from Net.Connect (copy the exact string from the code into the check) and `!Net.I.Connecting && Net.I.NetworkIdle`; the host asserts its Say line equals `\"Third knocked, but the session is full (8 players, counting invites not yet answered).\"` once, then hangs every pending invite (`foreach (var e in Net.I.Pending.All.ToList()) Net.I.Hang(e.Id)`) and posts \"free\"; guest2 waits for it and continues with its existing Auth-refusal and join. Raise the role limit if the 7 gathers (2 s each on a silent row under the box) do not fit 180 s.",
   "owner": "lane net2 (R2b/R2c)",
   "files": "tools/smoketest/SmokeTest.cs.txt (Mp host ~11017, guest2 ~11222); scripts/Net.cs:696-705,922-1000 (read-only reference)",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "net-RV10",
   "title": "RaidsArenaAddsChecks proves the adds live only at level 1; the L6 slot with a heavy (the first heavy, its 2600 u form-up, 0.5 s arrival, 9 s formation) is table-only",
   "kind": "review",
   "rung": "solo",
   "checks": "RaidsArenaAddsChecks (15060-15130); RaidsAddsTableChecks (15005)",
   "evidence": "raids_squads_adds.md:373 N12 wants a LIVE arrival check with a heavy in the slot; SmokeTest.cs.txt:15080-15084 asserts `Missions.Level == 1 && adds.Count == 2 && all Webifier`, and the ledger (J3 POST) admits only the table for L6+. Raids.TickGarrison (Raids.cs:79-103) reads `Missions.Level` through GarrisonBrief and `Missions.ForLevel(b.Level).AddsFloor`, so a level-6 deal is [HLL] on the Rusty (the check's own Sch(6, 2) == \"[HLL]\"). FIX: after the L1 block (before N13), with the beam stood down: set the arena's level to 6 the way the harness sets a mission level elsewhere (grep `Missions.Level =` / `SetLevel` in SmokeTest for the setter the failed-raid check uses), `H.RestartGarrison(); Raids.Held = false;` and assert within 30 frames: 3 raiders in one squad, kinds {Gunship, Webifier, Webifier} (by `Enemies.Of(r.Kind).Id`), squad anchor `2600 +- 5%` from the party centre (FormAt(W,W,G) == 2600 per the table check), `!formed` never true for 9 s, and `r.Worth == 18` on the gunship / 6 on the webifiers; then wipe, `Raids.Held = true`, restore the level. Keep N13 on the L1 slot as written.",
   "owner": "lane raids (J3)",
   "files": "tools/smoketest/SmokeTest.cs.txt:15060-15130",
   "unblocks": 2,
   "kept": true
  },
  {
   "id": "net-RV11",
   "title": "Listener rate meter never forgets an address; connections are served without bound before any check",
   "kind": "review",
   "rung": "quick",
   "checks": "none today (a solo check on Listener is not reachable: the class is private); the pure rule below gets one",
   "evidence": "Rendezvous.cs:601 `_knocks` (IPAddress -> Queue<ulong>) gains a key for every address that ever knocked and never removes one (683-688 only dequeues stamps inside a queue); a long session on a public port grows it without limit. Rendezvous.cs:626 `_ = Task.Run(() => Serve(client));` starts a 3 s ReadRecord task per accepted connection before any rate or build check, unbounded. FIX (1) in Weigh after the `while (...) seen.Dequeue();` loop add `foreach (var dead in _knocks.Where(kv => kv.Value.Count == 0 && !ReferenceEquals(kv.Value, seen)).Select(kv => kv.Key).ToList()) _knocks.Remove(dead);` (a queue empties as its minute passes); (2) in Accept, a `SemaphoreSlim _serving = new(16)` taken before `Task.Run(Serve)` and released in Serve's `finally`; a 17th concurrent connection is closed unread (`client.Dispose()`). CHECK: move the meter into a pure `Rendezvous.KnockMeter` (a static `bool Allow(Dictionary<IPAddress,Queue<ulong>> book, IPAddress from, ulong now)`) and prove it solo from 3 varied addresses and stamps: the 21st knock inside 60 s is refused, the 21st with the first 60.5 s old is allowed, and an address quiet for a minute is gone from the book.",
   "owner": "lane net (R1 J10)",
   "files": "scripts/Rendezvous.cs:594-704; tools/smoketest/SmokeTest.cs.txt (Codes() or a new R1KnockMeterChecks beside it, 362)",
   "unblocks": 1,
   "kept": false
  }
 ]
}