# Traps: symptom -> cause -> where

Grep this file by symptom before any diagnosis. Until the post-release cleanup moves them here, the traps live in
`docs/DESIGN.md` (grep "trap") and each lane ledger's DESIGN lines; a new trap is one line here in the same commit as its fix.
